import{

    SET_BOOKS
    
} from "../constants"

const initState = {


}


export const reducer = (state=initState, action={}) => {

    switch(action.type){

        case A: 
            return {...state}



        default: 
            return {...state}
    }
}