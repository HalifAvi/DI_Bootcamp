
const initState = {



}

export const sndReducer = (state=initState, action={}) => {

    switch(action.type){

        case 'AAA': 
            return {...state}

        default: 
            return {...state}
    }
}